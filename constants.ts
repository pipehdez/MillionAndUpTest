const serverUrl = "https://api.coinlore.net/api";

const tickersEndpoint = serverUrl + "/tickers/";

export const endpoints = {
  tickers: tickersEndpoint,
};

export const queryKeys = {
  tickers: "tickers",
};
